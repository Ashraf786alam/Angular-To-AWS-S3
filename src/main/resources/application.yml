cloud:
  aws:
    credentials:
      access-key: // aws access key.
      secret-key: // aws secret key.
    region:
      static: us-east-2
    stack:
      auto: false
      


application:
  bucket:
    name: ashrafalam
    
server:
  port: 4000
